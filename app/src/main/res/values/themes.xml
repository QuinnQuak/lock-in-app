<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.FirstProject" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/lavender_primary</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/sage_secondary</item>
        <item name="colorOnSecondary">@color/white</item>
        <!-- Window background, matched to the Compose theme's background so there's no flash on launch. -->
        <item name="android:windowBackground">@color/calm_background_light</item>
    </style>
</resources>
